{foreach $rssFeed as $item}
    <p><a href={$item['url']}>{$item['title']}</a></p>
    {first}
        <div>{$item['description']|noescape}</div>
        <img src={$item['image']}>
    {/first}
{/foreach}